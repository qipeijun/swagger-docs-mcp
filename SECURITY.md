# 安全策略

## 支持范围

安全修复随当前最新版本发布。项目处于 `0.x` 阶段时，仅维护最新次版本，不为旧次版本提供长期安全补丁。

## 报告漏洞

请通过 GitHub Security Advisory 的私密报告入口提交漏洞，不要在公开 Issue 中披露可复现的敏感细节。报告应包含影响版本、复现步骤、预期影响和建议修复方向。

## 网络信任边界

`swagger-docs-mcp` 会对用户明确传入的 `docsUrl` 发起 HTTP(S) GET 请求，因此默认可访问运行进程能够访问的公网、内网和本机地址。它是只读文档客户端，不是 SSRF 隔离代理；不要把不受信任用户提供的 URL 直接转交给该进程。

需要限制目标来源时，设置逗号分隔的精确 origin 白名单：

```bash
SWAGGER_DOCS_ALLOWED_ORIGINS=https://api.example.com,http://127.0.0.1:8080 npx --yes swagger-docs-mcp@latest
```

每项只能包含协议、主机和可选端口。配置后，入口、同源发现地址和重定向目标都必须属于允许来源。

其他固定边界：

- 只接受 HTTP(S)，拒绝 URL 内嵌用户名或密码。
- 最多跟随 3 次同源重定向，拒绝跨 origin 重定向和跨 origin 文档发现。
- 单次请求默认超时 10 秒，响应体默认上限 20 MB。
- 不请求 Swagger 中描述的业务 API，也不请求外部 `$ref`。
- 不保存文档地址、文档内容或跨调用缓存。
- MCP 结果会回传用于溯源的文档地址；不要在 URL 查询参数中放置口令、令牌或其他凭据。

客户端自动配置会调用对应官方 CLI。部分 CLI 在检查 MCP 配置时可能启动已配置的 stdio 服务；本项目将检查放在系统临时目录并设置 15 秒超时，但使用者仍应只对可信客户端配置执行自动配置。
